<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Book</title>
  <style>
    body {
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      background: linear-gradient(135deg, #74b9ff, #a29bfe);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .update-card {
      background: white;
      padding: 40px 30px;
      border-radius: 16px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.15);
      width: 100%;
      max-width: 450px;
      animation: fadeIn 0.6s ease-in-out;
    }

    .update-card h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #0984e3;
      font-size: 26px;
    }

    form {
      display: grid;
      gap: 20px;
    }

    .form-group {
      position: relative;
    }

    input {
      width: 100%;
      padding: 14px 12px;
      border: 1px solid #dcdde1;
      border-radius: 10px;
      font-size: 15px;
      outline: none;
      transition: border 0.3s, box-shadow 0.3s;
      background: none;
    }

    input:focus {
      border: 1px solid #0984e3;
      box-shadow: 0 0 6px rgba(9, 132, 227, 0.4);
    }

    label {
      position: absolute;
      top: 14px;
      left: 12px;
      color: #636e72;
      font-size: 14px;
      pointer-events: none;
      transition: 0.3s;
      background: white;
      padding: 0 5px;
    }

    input:focus + label,
    input:not(:placeholder-shown) + label {
      top: -8px;
      left: 10px;
      font-size: 12px;
      color: #0984e3;
    }

    button {
      padding: 14px;
      font-size: 16px;
      background: #0984e3;
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      font-weight: bold;
      transition: background 0.3s, transform 0.2s;
    }

    button:hover {
      background: #74b9ff;
      transform: scale(1.02);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(-15px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <div class="update-card">
    <h2>✏️  Update Book</h2>
    <form method="post" action="/updateBook">
      <input type="hidden" name="id" value="<%= singleData.id %>">

      <div class="form-group">
        <input type="text" name="title" value="<%= singleData.title %>" placeholder=" " required>
        <label>Book Title</label>
      </div>

      <div class="form-group">
        <input type="text" name="name" value="<%= singleData.name %>" placeholder=" " required>
        <label>Author Name</label>
      </div>

      <div class="form-group">
        <input type="number" name="quntity" value="<%= singleData.quntity %>" placeholder=" " required>
        <label>Quantity</label>
      </div>

      <div class="form-group">
        <input type="number" name="price" value="<%= singleData.price %>" placeholder=" " required>
        <label>Price</label>
      </div>

      <button type="submit">Update Book</button>
    </form>
  </div>
</body>

</html>
